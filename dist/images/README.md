# Images to add

Drop the real photos here with these exact names, then uncomment the
matching `background: url(...)` lines noted in the components:

- `hero.jpg`    — hero background (latte / cafe shot). Used in Hero.astro
- `story.jpg`   — pour-over / brewing photo. Used in Story.astro
- `strip-1.jpg`, `strip-2.jpg`, `strip-3.jpg` — photo strip. Used in Visit.astro

Until then, tasteful gradient placeholders render in their place, so the
site works out of the box.
