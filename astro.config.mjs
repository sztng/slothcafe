// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Static output — the whole informational site pre-renders to plain HTML.
  // When you go live, set `site` to the real domain for correct canonical URLs.
  // site: 'https://www.slothandmessenger.coffee',
});
